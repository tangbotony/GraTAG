[NACOS]
REGISTRATION_SWITCH = ${registration_switch}
LOCAL_CONFIG = ${local_config}
SERVER_ADDRESSES = ${server_address}
NAMESPACE = ${namespace}
AK = ${ak}
SK = ${sk}
DATA_ID = ${data_id}
GROUP = ${group}
LOG_DIR = ${log_dir}
SERVICE_NAME=${service_name}
CLUSTER_NAME=${cluster_name}